// Matching the style of the other components in the project, we will create a simple TodoList component that displays a list of todos. This component will accept an array of todo items as props and render them in a styled list format.
// The lefthand side of the page will display the list while clicking on a todo item will show its details on the righthand side
// this portion of the code